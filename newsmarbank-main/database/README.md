# Database Layer

This directory holds the database schema definitions and basic data seeding tools.

## MongoDB (Recommended)
- **schemas/**: Contains JSON or JS representations of the Mongoose models.
- **seed/**: Pre-configured seed data for sandbox testing.
- **migrations/**: Scripts to run MongoDB migrations when deploying.

## SQL (Optional)
Fallback or optional SQL schema equivalents.
